name: Sync labels (test)

on:
  workflow_dispatch: {}
  push:
    paths:
      - ".github/labels.yml"

# (Opcional) Permisos mínimos para este workflow.
permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Sync labels to selected repositories (no prune)
        uses: micnncim/action-label-syncer@v1
        with:
          manifest: .github/labels.yml
          repository: |
            treew-inc/pim-api-product-classifier
            treew-inc/pim-api-apibulkimages
            treew-inc/pim-api-catalogsearchapi
            treew-inc/pim-api-gtinservice
            treew-inc/pim-api-productscatalogapi
            treew-inc/pim-api-productssync
            treew-inc/pim-api-treewClassifier
            treew-inc/pim-web-catalogfrontend
          prune: false                 # No elimina etiquetas que no estén en labels.yml
          token: ${{ secrets.PERSONAL_TOKEN }}  # PAT con Write en Issues/metadata
        continue-on-error: true         # Si un repo falla, sigue con los demás
