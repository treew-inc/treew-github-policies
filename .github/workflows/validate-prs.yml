name: Validar PR

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read
  pull-requests: read

jobs:
  validate-pr:
    name: Verificar cumplimiento del Pull Request
    runs-on: ubuntu-latest

    steps:
      - name: 📋 Validar título del PR
        uses: amannn/action-semantic-pull-request@v5
        with:
          types: |
            feat
            fix
            chore
            docs
            style
            refactor
            perf
            test
            revert
          scopes: ''
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 📄 Verificar contenido obligatorio del cuerpo del PR (flexible)
        uses: actions/github-script@v7
        with:
          script: |
            const body = github.context.payload.pull_request?.body || '';
            const requiredSections = [
              { label: "## Descripción", regex: /^##\s*Descripción\b/m },
              { label: "## Referencias", regex: /^##\s*Referencias\b/m },
              { label: "## Checklist", regex: /^##\s*Checklist\b/m }
            ];
            const missing = requiredSections
              .filter(section => !section.regex.test(body))
              .map(section => section.label);
            if (missing.length > 0) {
              core.setFailed(`Faltan secciones requeridas en el cuerpo del PR: ${missing.join(', ')}`);
            }

      - name: ✅ Validación exitosa
        if: success()
        run: echo "✅ Pull Request cumple con la directiva institucional."
